= form_for @business_user do |f|
  - if @business_user.errors.any?
    #error_explanation
      %h2= "#{pluralize(@business_user.errors.count, "error")} prohibited this business_user from being saved:"
      %ul
        - @business_user.errors.full_messages.each do |msg|
          %li= msg

  .actions
    = f.submit 'Save'
